# 05_deliver/input/02_store

Output from the `02_store` stage — schema snapshots, migration history, and any
data exports produced directly from the store layer (not from Cypher analysis queries).

Currently a placeholder. Future use:
- Neo4j schema diff reports
- Bootstrap/migration execution logs
